import { teal, blueGrey } from '@material-ui/core/colors';

const theme = {
  palette: {
    primary: blueGrey,
    secondary: teal,
  },
  typography: {
    useNextVariants: true,
  },
};

export default theme;
